# デザイン

このドキュメントは、画面を実装するときに守る見た目と表示規則を定義します。  
利用者から見える動作と文言は[仕様](docs/specification.md)、内部の実現方法は[アーキテクチャ](docs/architecture.md)、検証方法は[テスト](docs/testing.md)に定義します。  

## 情報を増やさず、Gitの状態を迷わず読める画面にする

- macOSの標準的な密度と操作感を基準にし、装飾より作業内容を優先します。
- 日常操作では変更、ファイル、差分、実行する操作だけを見せ、詳しいGit情報は必要な場面で表示します。
- 同じ役割の部品には同じ見た目を使い、画面ごとの派生デザインを作りません。
- 警告、失敗、復旧が必要な状態は、通常状態との違いが見ただけで分かるようにします。
- 新しい色や影を直接追加せず、`src/styles.css`にある意味に基づくトークンを再利用します。

## 参考画面

- [ファイル差分](docs/assets/diff.png)
- [ファイル編集](docs/assets/editor.png)
- [履歴](docs/assets/history.png)
- [活動](docs/assets/activity.png)
- [設定](docs/assets/settings.png)

参考画面と仕様が異なる場合は、現在の仕様を優先します。  

## 配色は役割をトークンで表す

- ライト、ダーク、システム設定への追従を同じ用途別トークンで実現します。
- 背景は`--surface-*`、文字は`--text-*`、境界線は`--border-*`、影は`--shadow-*`を使用します。
- 主要操作と強調する選択状態には`--accent`系、一覧の選択状態には`--list-selection-*`を使用します。
- 成功、警告、危険、情報は、それぞれの状態を表す色と控えめな背景色を組み合わせます。
- 色だけに意味を持たせず、アイコン、記号、文言、形状のいずれかを併用します。
- コントラストを上げるmacOSの設定でも、境界と選択状態が背景へ埋もれないようにします。

## 書体と密度

- 通常の画面にはシステムUIフォント、差分とエディターには等幅フォントを使用します。
- 文字サイズは80%、90%、100%、110%、120%に対応し、100%では差分とエディターの本文を13px、行の高さを20pxにします。
- 通常の入力欄と選択欄は高さ36pxを基準とし、アイコンだけの操作は周囲の密度に合わせて小さくします。
- 見出しを重ねて現在地を繰り返さず、ペインの内容を広く使います。
- 件数は対象名の直後に置き、画面右端だけを見なければ状態が分からない配置を避けます。

## レイアウト

- 標準の画面サイズは1180×760、最小サイズは860×560とします。
- 画面上部はペインで分割せず、リポジトリ情報と画面移動を全幅の共通ヘッダーへまとめます。
- 変更と履歴は左側で対象を選び、右側を差分や詳細へ広く割り当てます。
- 活動は分析と操作履歴、設定はカテゴリと詳細を左右に分け、それぞれの内容をペイン内でスクロールします。
- ウィンドウ全体をスクロールさせず、見出しと主要操作を残したまま内容領域だけをスクロールします。
- 最小サイズでも主要操作を隠さず、横スクロールを発生させません。

## 待機表示

- 読み込みや送信の途中を表す可視文言と骨格プレースホルダーは表示しません。
- 処理開始から1秒未満は待機アイコンも表示せず、現在の内容を保つか表示領域の寸法だけを維持します。
- 1秒以上続く場合だけ、共通の回転アイコンを表示します。
- ボタンでは通常時の操作名を維持し、その前にアイコンを置きます。
- 活動に記録する実行状態、完了や失敗の結果、利用者の対応が必要な通知は待機表示に含めません。

## 一覧と選択状態

- 変更ファイル、履歴のコミット、活動の操作は、角を丸めない全幅の中立色背景で選択を示します。
- ホバーは選択より弱い背景、キーボードフォーカスは`--focus`の枠で区別します。
- 選択背景の上でも、ファイル状態、履歴レーン、参照、進行状態を表す色を維持します。
- 矢印キーで履歴を移動している間は静止中のポインターによるホバーを表示せず、ポインターを動かした後に戻します。
- ファイルの選択背景は行全体で同時に切り替え、チェックボックス、ファイル情報、操作メニューの間に時差を作りません。
- ファイル状態は、追加、変更、削除、名前変更、バイナリ、競合をアイコンと色で区別します。
- 行末の追加操作は、行のホバーまたはフォーカス時だけ見せても、キーボードから利用できる状態を保ちます。

## タブと分割選択

- 同じグループのタブは隙間なく密着させ、外周だけに枠線と角丸を付けます。
- `segmented`は2pxの内側余白、1pxの外周線、9pxの外側角丸を基準とし、タブ間に仕切り線を置きません。
- 強調する選択状態には`--interactive-selected-*`、表示形式などの中立的な選択には`--list-selection-*`を使用します。
- 競合ブロックの選択には、決定ボタンと同じ強調色を使用します。
- 見た目の選択項目と`aria-selected`または`aria-pressed`が必ず一致するようにします。

## 差分とファイル表示

- 追加行と削除行は、行全体の背景、ガター、`+`／`-`の記号を組み合わせ、ライトとダークの両方で明確に区別します。
- 差分内の行選択は、追加と削除を表す色を消さない青系の選択色で示します。
- ファイルヘッダーには状態アイコン、パス、追加行数、削除行数を同じ順序で表示します。
- ファイル名とコード本文の開始位置を揃え、複数ファイルのヘッダーは同じ高さと背景を使用します。
- 画像差分も既存の面、境界線、余白を使い、専用の見た目を作りません。
- 画像プレビューの背景は画像ごとの明るさに応じて白または黒を選び、枠内の余白を含む全体を選択した背景に合わせたチェック柄で示します。
- 画像の読み込みとデコードが完了するまでは、プレビュー領域を表示しません。
- 履歴の画像差分では、ファイルごとの共通ヘッダーにプレビュー操作を置き、表示状態をファイル単位で管理します。

## 履歴グラフ

- コミットの接続関係を追えることを装飾より優先し、レーンの位置と色を行の選択状態で変えません。
- 同じレーンは選択行の前後でも同じ色で連続させ、未コミット区間だけを低彩度のグレーで区別します。
- コミットは円形のノード、分岐と合流はレーン間を追える接続線で表示します。
- ブランチ、リモートブランチ、タグは小さなチップで表示し、タグはアイコンと警告系の色で区別します。

## ダイアログとツールチップ

- 通常のダイアログは幅580pxを基準とし、ヘッダー、スクロールする本文、右寄せの固定フッターで構成します。
- 背景は半透明の暗い面とぼかし、ダイアログは`--surface-overlay`と`--shadow-sheet`で前後関係を示します。
- 閉じる操作は右上へ置き、主要操作より目立たせません。
- ツールチップはトリガーの中央を指す矢印を持ち、通常は8px離して表示します。
- ツールチップは最大幅280px、8pxの角丸とし、ライトでは濃色、ダークでは明色の面を使います。
- アイコンだけの操作には、ポインターとキーボードのどちらでも同じツールチップを表示します。

## アクセシビリティを見た目と分離しない

- 選択、現在位置、開閉、処理中、無効の各状態を視覚表現とARIA属性で一致させます。
- キーボードフォーカスは常に見える枠で示し、選択背景だけで代用しません。
- アイコンだけの操作にはアクセシブル名を付け、装飾アイコンは読み上げ対象から外します。
- 状態色を使う場所では、文字、形、記号のいずれかでも同じ意味を伝えます。
- コントラストや文字サイズを変更しても、操作の欠け、重なり、状態の逆転を起こしません。
