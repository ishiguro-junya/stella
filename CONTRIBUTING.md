# コントリビュートガイドライン

アプリへの不具合報告、機能提案、コードやドキュメントの変更を歓迎します。  
変更を提出する前に、このガイドの手順と権利条件を確認してください。  

## 開発を始める

開発には、Apple Siliconを搭載したmacOS 26.0以降のMacとmiseを使用します。  
リポジトリを取得した後、次のコマンドで依存パッケージ、Gitフック、開発用データを準備し、アプリを起動します。  

```sh
mise install
mise run setup
mise run dev
```

履歴・差分確認用と選手ごとの開発用リポジトリ、およびリモートはローカルに作成されるため、実在するリポジトリや外部ネットワークへ接続しません。  
初期状態へ戻す場合は`mise run reset`を実行します。  
AIエージェントを利用して開発する場合は、[READMEに掲載しているエージェントスキル](README.md#-エージェントスキル)の利用を推奨します。  

依存パッケージを更新する場合は`mise run update`を実行し、更新後に`mise run ci`で検証します。  

### 並行開発する

複数の変更を並行して開発を進める場合は、次の順序で準備します。  

1. リポジトリ直下の`worktrees/`配下へworktreeを作成します。
2. 作成したworktree内で`mise run setup`を実行します。
3. コードを変更する前に`mise run test:visual-regression --update`を一度実行し、そのworktree用の基準画像を生成します。
4. `mise run dev`または必要なテストを実行します。

2個目以降のworktreeでもポート指定は不要です。  
開発アプリ、E2E、ビジュアルリグレッション、スクリーンショットは用途別の範囲から空いているポートを自動選択します。  
各worktreeの`node_modules/`、`target/`、`tmp/`は独立し、テスト用リポジトリ、Tauriの実行状態、ビジュアルリグレッションの基準画像と比較画像も共有しません。  
Rustを構築する開発アプリと検査は、全worktreeで1つのネイティブ実行枠を共有します。  
別のネイティブ処理が動作中の場合は終了まで待機し、Cargoは既定で2ジョブまで使用します。  
開発アプリは終了するまで枠を保持しますが、TypeScriptの型検査やフロントエンドの単体テストは別のworktreeでも並行実行できます。  

## 必要な検査を行う

変更箇所に近いテストから実行し、提出前に変更を含む範囲の検査まで行います。  
変更内容ごとのコマンドは[テスト](.agents/references/workflows/testing.md)を参照してください。  
変更をレビューする場合は、[レビュー](.agents/references/workflows/review.md)の方針と完了基準に従います。  

コード変更によって仕様、利用方法、外部インターフェース、構築手順、運用手順が変わる場合は、関連するドキュメントも更新します。  
ドキュメントとコードコメントは[ドキュメントの書き方](.agents/references/workflows/writing.md)に従います。  
画面文言を変更する場合は、日本語と英語の翻訳を同時に更新します。  

## コミットを作成する

コミットは変更理由ごとに分け、Conventional Commits形式で記述します。  
`type`と任意の`scope`は英語、説明は日本語にします。  
プッシュ前には`mise run ci`、コミット時にはCocogittoによるコミットメッセージの検査が実行されます。  

## プルリクエストを作成する

最新の`main`から作業用ブランチを作成し、`main`宛てにプルリクエストを作成します。  
説明には変更の目的、主な変更内容、実行した検査を記載します。  
画面の見た目を変更した場合は、変更前後を判断できる画像も添付します。  

## 不具合や機能を提案する

不具合報告と機能提案は[GitHub Issues](https://github.com/ishiguro-junya/stella/issues)へ登録します。  
不具合報告には再現手順、期待する動作、実際の動作、このアプリとmacOSのバージョンを記載します。  

## 権利条件

貢献内容を送信した方は、採用された内容の著作権（著作権法第27条および第28条の権利を含みます）をJunya Ishiguroへ無償で譲渡することに同意したものとします。  
また、法令上許される範囲で著作者人格権を行使しないことと、この同意に必要な権利を有していることにも同意したものとします。  
