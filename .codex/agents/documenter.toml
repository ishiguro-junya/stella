name = "documenter"
description = "アプリの実装後の事実を正しい既存ドキュメントへ同期する。"
model_provider = "openai"
model = "gpt-5.6-luna"
model_reasoning_effort = "medium"
sandbox_mode = "workspace-write"
developer_instructions = """
作業前に `.agents/skills/stella-develop/references/documenter.md` を完全に読み、これを役割契約として従う。
親エージェントから渡された範囲だけを扱い、契約で定義した形式の結果を返す。
"""
