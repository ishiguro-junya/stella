lefthook: mise exec -- lefthook
glob_matcher: doublestar

commit-msg:
  commands:
    conventional-commit:
      run: cog verify --file "{1}"

pre-push:
  commands:
    ci:
      run: mise run ci --smoke
